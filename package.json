{
  "name": "serverless-video-transcoder",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "jest": {
    "setupFiles": [
      "dotenv/config"
    ]
  },
  "scripts": {
    "start": "sls offline start --skipCacheInvalidation --STAGE test --region localhost",
    "deploy": "sls deploy --STAGE test --region us-east-1",
    "remove": "sls remove --STAGE test --region us-east-1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "serverless-dotenv-plugin": "^3.0.0"
  }
}
